import { Sample } from "sample"

// const getVisibleTodos = (todos: Todo[], filter: boolean) =>
//   filter ? todos.filter(v => !v.completed) : todos

const getSample = (sample: Sample) => {
  return sample;
}

export default {
  getSample
}
